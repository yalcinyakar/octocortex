[project]
name = "octocortex"
version = "0.1.0"
description = "Global-workspace plus distributed SNN architecture simulator"
requires-python = ">=3.11"

[tool.setuptools.package-data]
octocortex = ["static/*"]

